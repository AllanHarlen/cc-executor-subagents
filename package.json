{
  "name": "cc-executor-subagents",
  "version": "2.3.0",
  "private": true,
  "type": "module",
  "description": "Infraestrutura de testes do plugin cc-executor-subagents. Sem dependencia de runtime: o codigo de producao usa apenas builtins do Node.",
  "license": "MIT",
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "devDependencies": {
    "fast-check": "4.9.0"
  }
}
